{
  "name": "aborg-frontend",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "description": "Web UI for ABorganizer — Svelte 5 + Vite + TS. One codebase serves the browser SPA at https://localhost:8429/ and the macOS menubar app's WKWebView embed via the ?host=menubar host-detection contract (ADR-0040).",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "check": "svelte-check --tsconfig ./tsconfig.json",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^4.0.0",
    "@tsconfig/svelte": "^5.0.4",
    "svelte": "^5.0.0",
    "svelte-check": "^4.0.0",
    "tslib": "^2.7.0",
    "typescript": "^5.6.0",
    "vite": "^8.0.13"
  }
}
