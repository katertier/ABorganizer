// Ambient declarations for the SPA. Currently empty; module
// augmentations (e.g. extending `Window` with non-standard
// WKWebView surfaces beyond the one declared in `host.ts`)
// land here as the surface grows.

export {};
